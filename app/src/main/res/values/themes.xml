<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Material3 base theme -->
    <style name="Base.Theme.QuranDaily" parent="Theme.Material3.Light.NoActionBar">
        <!-- Disable automatic button tinting -->
        <item name="android:fontFamily">@font/poppins</item>
        <item name="fontFamily">@font/poppins</item>
        <item name="materialButtonStyle">@style/NoTintButton</item>
    </style>

    <style name="Theme.QuranDaily" parent="Base.Theme.QuranDaily" />

    <!-- Button style that removes tint -->
    <style name="NoTintButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
        <item name="android:backgroundTint">@null</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>

</resources>



